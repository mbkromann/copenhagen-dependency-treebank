sub extraction_path {
	my $self = shift;
	my $node = shift;
	my $enode = shift;
	my $list = shift || [];

	if (! defined($enode)) {
	}

}



sub spans {
	my $self = shift;
	my $lsite = shift;
	my $node = shift;
}
