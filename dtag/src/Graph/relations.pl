=item $graph->relations() 

Extract list with all relations in graph. ???

=cut

# $graph->relations({'comp' => ['subj', ...], 'adj' => ['mod', ...]}) 

# $template = 

sub relations {
	my $self = shift;
	my $template = shift;

}

sub rel_comps {
	my $self = shift;
	my $edges = shift;
	my $node = shift;

	

}

sub rel_agovs {
	my $self = shift;
	my $edges = shift;
	my $node = shift;
}


